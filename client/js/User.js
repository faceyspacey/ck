/*
isAdmin = function(){
    console.log('isAdmin');
    return Roles.userIsInRole(Meteor.userId(), ['admin']);
}


User = (function(){
    var _id;
    var emails = [];
    var profile = {};

    function isAdmin(){
        console.log('isAdmin');
        return Roles.userIsInRole(Meteor.userId(), ['admin']);
    }

    return {
        isAdmin: isAdmin,
    };
})();

window.User = User;*/

